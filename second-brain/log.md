# Log delle sessioni

Cronologia sintetica, una riga (o poche) per sessione. Dettagli vanno negli altri file collegati.

- **2026-08-07**: Creato il second brain ([index.md](index.md), [progetto.md](progetto.md), [decisioni.md](decisioni.md), [preferenze.md](preferenze.md)). Nessun altro lavoro svolto in questa sessione.
- **2026-08-07**: Implementata animazione reveal-on-scroll orizzontale per i testi della homepage (hero nascosta finché non si scrolla, testo a sinistra entra da sinistra, testo a destra entra da destra). Vedi [decisioni.md](decisioni.md).
- **2026-08-07**: Rallentata l'animazione a mosaico del video "Customised online courses" e resa scroll-driven/ripetibile (riparte ogni volta che si esce e si rientra nel viewport). Vedi [decisioni.md](decisioni.md).
- **2026-08-07**: Fixato bug in `index.astro`: le colonne destre delle sezioni "Video & Utilities" e "About me" avevano `reveal-left` invece di `reveal-right`, per cui l'animazione dx→sx era sparita solo lì. Vedi [decisioni.md](decisioni.md).
- **2026-08-07**: Resa ripetibile l'animazione `.reveal-left`/`.reveal-right` (e `.reveal`): non più one-shot, ora si nasconde scrollando via e rianima ogni volta che rientra in viewport. Verificato con test Playwright headless. Vedi [decisioni.md](decisioni.md).
- **2026-08-08**: Ridisegnata la sezione "About me" della home (`index.astro`): aggiunto ritratto (riusato da `/about/`) con overlay hover "Read my story", badge di credibilità (NASA APOD, Optolong 2021, dal 2018) e icone social (Instagram/Facebook/YouTube) accanto al bottone "Contact me". Verificato visivamente con screenshot Playwright. Vedi [decisioni.md](decisioni.md).
- **2026-08-08**: Il testo di `/about/` non aveva un file `.md`; creata nuova content collection `about` e `src/content/about/bio.md`, `index.astro` ora renderizza il markdown invece del testo hardcoded. Vedi [decisioni.md](decisioni.md).
- **2026-08-08**: Fix latenza immagini all'apertura della home: le foto della hero slideshow erano servite non ottimizzate (fino a 25MB/immagine). Aggiunta ottimizzazione via `getImage` (webp, 1920px) + preload della prima slide + fetchpriority/loading su tutte. Vedi [decisioni.md](decisioni.md).
- **2026-08-08**: Aggiunto switch lingua IT/EN con bandierine (toggle client-side via `LangSwitcher.astro`, italiano default, nessun routing separato). Tradotto tutto il sito in italiano: nav, footer, tutte le pagine statiche, tutte le 38 schede portfolio + bio (nuove collection `portfolioIt`/`aboutIt`), termini tecnici (stacking, plate solve, ecc.) lasciati in inglese su richiesta. Build verificata senza errori. Vedi [decisioni.md](decisioni.md).
- **2026-08-08**: Consolidate le collection gemelle `portfolioIt`/`aboutIt` in un unico file per entry: `bodyIt`/`titleIt` come campi frontmatter (resi in HTML con `marked`), niente più cartelle `*-it/` separate né rischio di immagine/campi disallineati tra lingue (bug reale già visto su andromeda). Build verificata. Vedi [decisioni.md](decisioni.md).
- **2026-08-09**: Importati dati di acquisizione AstroBin (telescopio, camera, filtri, tempo di integrazione) per tutte le 38 foto del portfolio, via scraping Node+Playwright di un endpoint pubblico non documentato di AstroBin (API ufficiale chiusa alle nuove richieste). Nuova sezione "Equipment & Acquisition" bilingue in `[object].astro`. Estesa poi con luogo, data, Bortle, emisfero, coordinate celesti, campionamento, raggio del campo, guida, accessori, software. Vedi [decisioni.md](decisioni.md) per il percorso completo.
- **2026-08-09**: Installata la skill esterna "UI/UX Pro Max" (`npx ui-ux-pro-max-cli init --ai claude`, confermato dall'utente dopo verifica del pacchetto) in `.claude/skills/` — 7 sotto-skill per design UI/UX/brand/design-system. Vedi [decisioni.md](decisioni.md).
- **2026-08-09**: Ridisegnata la sezione "Equipment & Acquisition" delle schede portfolio (era percepita come "AI slop": griglia piatta senza gerarchia). Primo tentativo (card con icone + stat in evidenza) bocciato dall'utente ("è peggio di prima"). Installata la skill ufficiale Anthropic `frontend-design` e riprogettato da zero come readout FITS/console da telescopio (monospace ambra-su-nero, formato `CHIAVE = valore / commento`, cursore lampeggiante), radicato nel vocabolario reale del dominio invece che in pattern UI generici. Poi rifinito su feedback dell'utente: tolti `=`, apici e cursore finale (simboli decorativi inutili), campi multi-valore (filtri/accessori/software/telescopi) impilati verticalmente invece che su una riga con virgole. Verificato con build + screenshot Playwright (dati completi/parziali, mobile, IT/EN). Vedi [decisioni.md](decisioni.md).
- **2026-08-09**: L'utente ha notato dati AstroBin non aggiornati (Andromeda) nonostante la sincronizzazione già esistente (integrazione `astrobinSync` in `astro.config.mjs`, gira a ogni `astro dev`/`build`); la cache era valida per sempre finché il link AstroBin non cambiava. Aggiunta scadenza a 24h (`_fetchedAt` + `CACHE_TTL_MS` in `fetch-astrobin.mjs`): ora i dati si riaggiornano da soli entro un giorno anche se su AstroBin cambia solo l'equipaggiamento/le sessioni, senza toccare il link. Rilanciato lo script su tutte le 38 foto per portarle al nuovo formato. Vedi [decisioni.md](decisioni.md).
