# Decisioni

Decisioni tecniche/architetturali prese nel tempo, con motivazione.

## 2026-08-07 — Introdotto il "second brain"
Creata la cartella `second-brain/` con file .md collegati tra loro per mantenere memoria persistente del lavoro su questo progetto, letta a inizio chat e aggiornata a fine chat.

**Perché:** l'utente vuole che io ricordi il contesto del lavoro fatto insieme in questa cartella, oltre alla memoria di sistema esterna al progetto.

**Come si applica:** vedi [index.md](index.md) per il funzionamento; questa istruzione è anche richiamata in `CLAUDE.md` per essere seguita automaticamente in ogni sessione futura.

## 2026-08-07 — Sistema di reveal-on-scroll con direzione orizzontale per il testo

Esisteva già un sistema `.reveal` (fade + slide verticale) gestito da `ScrollReveal.astro` via `IntersectionObserver`, applicato a sezioni sotto la hero. Aggiunte due nuove classi `.reveal-left`/`.reveal-right` in `src/styles/global.css` (slide orizzontale da sinistra o da destra) e usate al posto di `.reveal` su tutti i blocchi di testo allineati rispettivamente a sinistra/destra in `src/pages/index.astro`.

Il titolo hero ("Valerio Avitabile" / "Astrophotography") è già visibile nel viewport al caricamento della pagina, quindi l'`IntersectionObserver` standard lo avrebbe rivelato subito invece che al primo scroll. Per questo ha una classe extra `.reveal-hero`: `ScrollReveal.astro` la esclude dall'observer e la rivela invece al primo evento di `scroll` della pagina (listener one-shot).

**Perché:** l'utente vuole che il testo della hero sia invisibile all'apertura della pagina e appaia solo quando l'utente inizia a scrollare, con logica di entrata coerente con l'allineamento (testo a sinistra entra da sinistra, testo a destra entra da destra).

**Come si applica:** per nuovi blocchi di testo con animazione in ingresso, usare `reveal-left`/`reveal-right` invece di `reveal` in base all'allineamento; per elementi già visibili al load che devono aspettare il primo scroll, aggiungere anche `reveal-hero`.

**Aggiornamento 2026-08-07:** l'utente ha chiesto di NON applicare questa animazione al titolo hero della home ("Valerio Avitabile" / "Astrophotography"): rimosse le classi `reveal-hero reveal-left` da quell'h1/p in `src/pages/index.astro`, che restano sempre visibili come prima. Il meccanismo `.reveal-hero` in `ScrollReveal.astro` resta comunque disponibile per un eventuale uso futuro altrove.

## 2026-08-07 — Animazione mosaico del video "Customised online courses" resa più lenta e ripetibile

Il video hero della sezione corsi (`.mosaic-reveal` + `.mosaic-tiles` in `src/pages/index.astro`, logica in `setupMosaic()` dentro `ScrollReveal.astro`) usava tile che si scoprivano una tantum (via `IntersectionObserver` con `unobserve` dopo il primo reveal), transizione 0.5s e delay casuale fino a 550ms.

Cambiato:
- `.mosaic-tile` transition da `0.5s` a `1.2s` ([global.css](../src/styles/global.css)).
- `maxDelay` in `setupMosaic()` da 550ms a 900ms.
- L'observer non fa più `unobserve`: resta attivo e su ogni uscita dal viewport (`!entry.isIntersecting`) richiama `hide()`, che rimuove `is-visible`, rimette in pausa/reset il video e rigenera i delay casuali dei tile, cosicché rientrando in vista l'animazione riparte sempre da capo.

**Perché:** l'utente voleva l'effetto a mosaico più lento e scroll-driven (ripetibile ogni volta che si scrolla via e si torna sulla sezione), non più un reveal one-shot.

**Come si applica:** per altre sezioni con `.mosaic-reveal`, questo comportamento (replay ad ogni ingresso in viewport) è ora lo standard; se in futuro serve un reveal one-shot altrove, va gestito con una variante dedicata invece di modificare questo meccanismo condiviso.

## 2026-08-07 — Fix: colonna destra delle sezioni "Video & Utilities" e "About me" non entrava più da destra

Nelle due sezioni finali di `src/pages/index.astro` (Video & Utilities, About me) entrambe le colonne del grid a due colonne usavano `reveal-left`, quindi la colonna di destra scorreva da sinistra invece che da destra (a differenza delle sezioni Portfolio/Corsi più in alto, che usano correttamente `reveal-left` + `reveal-right`). L'utente se n'è accorto perché l'animazione a scorrimento laterale (sx→dx per i testi a sinistra, dx→sx per quelli a destra) sembrava sparita.

**Perché:** probabile refuso in fase di scrittura del markup: le colonne destre di quelle due sezioni non hanno `text-right`/`items-end` come le prime due (layout diverso, testo comunque allineato a sinistra), il che ha reso più facile copiare `reveal-left` per errore invece di `reveal-right`.

**Come si applica:** la direzione di `reveal-left`/`reveal-right` dipende da dove si trova la colonna nel grid (sinistra/destra), non dall'allineamento del testo al suo interno — non vanno confusi. Se in futuro l'animazione sembra "sparita" solo su alcune sezioni, controllare prima quale classe reveal è applicata a ciascuna colonna in `index.astro`.

## 2026-08-08 — Ridisegnata la sezione "About me" della home per essere più accattivante/fruibile

La sezione era solo testo (eyebrow + titolo + bottone "More" + un paragrafo + bottone "Contact me"), poco invitante rispetto alle altre sezioni della home che usano `media-card` con immagini. Portata a un layout a due colonne come `/about/`:

- Colonna sinistra: ritratto (`assets/about/portrait.jpg`, stesso asset usato in `/about/`) dentro un `media-card` linkato a `/about/`, con overlay hover "Read my story" (stesso pattern delle altre card della home).
- Colonna destra: eyebrow, titolo invariato, nuova riga di badge di credibilità (`NASA APOD featured`, `Optolong 2021 winner`, `Shooting since 2018` — presi dai fatti già raccontati in `/about/`), bottone "Read my story" (rinominato da "More"), poi sotto un separatore la domanda di contatto, bottone "Contact me" e tre icone social (Instagram/Facebook/YouTube, da `site.social`) per invitare a seguire i canali.
- Aggiunta classe `.badge-pill` in `global.css` (coerente con `.eyebrow`/`.btn-accent`) per i badge.
- Le icone social riusano lo stesso stile visivo del footer ma con markup/hover Tailwind inline (accent color unico invece dei colori per-brand del footer) invece di condividere la classe scoped `.footer-icon` di `Footer.astro`.
- Colonne mantengono `reveal-left` (sinistra) / `reveal-right` (destra) coerentemente con [la voce sopra sulla direzione reveal](#2026-08-07--fix-colonna-destra-delle-sezioni-video--utilities-e-about-me-non-entrava-più-da-destra).

**Perché:** richiesta esplicita dell'utente di rendere la sezione più accattivante visivamente e di dare un invito più chiaro a leggere la storia personale e a seguire/contattare (portfolio "About me" prima era solo testo su sfondo scuro, poco invitante rispetto al resto della home).

**Come si applica:** per altre sezioni testuali della home che si vogliono rendere più coinvolgenti, questo è il pattern di riferimento (media-card con overlay + badge di credibilità + CTA + icone social), verificato visivamente con screenshot Playwright (stato normale e hover).

## 2026-08-08 — Testo di `/about/` spostato da hardcoded a content collection markdown

L'utente ha chiesto se la pagina `/about/` avesse un `.md` da qualche parte: non ce l'aveva, il testo della biografia era scritto direttamente come JSX/HTML dentro `src/pages/about/index.astro`. L'unica content collection esistente nel progetto era `portfolio` (`src/content/portfolio/*.md`, vedi `src/content.config.ts`), che usa il pattern `getCollection`/`render`/`<Content />` già visto in `src/pages/portfolio/[category]/[object].astro`.

Creata una nuova collection `about` (stesso `glob` loader, base `./src/content/about`, schema con solo `title: z.string()`), e un file `src/content/about/bio.md` con lo stesso testo che prima era hardcoded (incluso il link a `/courses/` come link markdown). `src/pages/about/index.astro` ora fa `getEntry('about', 'bio')` + `render(entry)` e mostra `<Content />` dentro il div `.prose-astro`, invece dei sette `<p>` scritti a mano.

**Perché:** rendere la biografia editabile come testo/markdown separato dal codice, coerente con come è già strutturato il resto dei contenuti testuali lunghi nel progetto (le schede portfolio).

**Come si applica:** per modificare il testo della pagina About in futuro, editare `src/content/about/bio.md`, non `index.astro`. Se in futuro serve un'altra pagina "a contenuto lungo" editabile fuori dal codice, questo è il pattern da seguire (nuova collection + file .md + `getEntry`/`render`/`<Content />`).

## 2026-08-07 — `.reveal`/`.reveal-left`/`.reveal-right` resi ripetibili (replay ad ogni ingresso in viewport)

L'observer in `ScrollReveal.astro` faceva `unobserve` dopo il primo reveal (one-shot): una volta apparso, il testo restava visibile per sempre, anche scrollando via e tornando indietro. L'utente vuole invece che, scrollando verso l'alto (via dalla sezione), il testo si nasconda di nuovo (tornando alla posizione traslata sx/dx con opacity 0) e rianimi da capo quando si rientra nella sezione scrollando in basso.

Cambiato l'observer da "aggiungi `is-visible` una volta e unobserve" a un toggle: `entry.target.classList.toggle('is-visible', entry.isIntersecting)`, senza mai fare unobserve. Stesso pattern già usato per `.mosaic-reveal` (vedi voce sopra). Verificato con test Playwright headless (scroll giù → nascondi scroll su → rianima scroll giù) che il replay funziona.

**Perché:** richiesta esplicita dell'utente di comportamento ripetibile/scroll-driven, non one-shot, per `reveal-left`/`reveal-right` (e di conseguenza anche `.reveal`, condividono lo stesso observer e nessuna pagina usa ancora `.reveal` da sola).

**Come si applica:** questo è ora lo standard per tutte le classi `.reveal*` (tranne `.reveal-hero`, che resta one-shot al primo scroll perché gestisce la hero già visibile al load). Nuovi blocchi con `reveal-left`/`reveal-right` erediteranno automaticamente il comportamento ripetibile.

## 2026-08-08 — Fix latenza immagini hero all'apertura del sito

L'utente segnalava latenza nel caricamento di alcune immagini all'apertura. Causa trovata: le foto delle hero slideshow (`src/assets/home/*.jpg`, usate in `HeroSlideshow.tsx` via `src/pages/index.astro`) venivano importate come asset Astro ma usate con `img.src` "grezzo" invece di passare da `Image`/`getImage`, quindi servite **non ottimizzate, a piena risoluzione originale** — fino a 25MB per singolo file (`wr134.jpg`), altre tra 4 e 15MB.

Fix in `src/pages/index.astro`: gli slide ora passano da `getImage({ src: img, width: 1920, format: 'webp', quality: 80 })` prima di essere passati a `HeroSlideshow`. Risultato dopo build: da 3.7–25MB per immagine a 78–374KB (webp), riduzione ~70-100x.

Aggiunto anche:
- `src/layouts/Layout.astro`: nuova prop opzionale `preloadImage`, emette `<link rel="preload" as="image" fetchpriority="high">` nell'head; passata da `index.astro` con `preloadImage={slides[0].src}` per far scaricare la prima immagine hero il prima possibile, in parallelo al parsing HTML.
- `src/components/HeroSlideshow.tsx`: aggiunti `fetchPriority` (`high` per la prima slide, `low` per le altre) e `decoding` (`sync`/`async`) sugli `<img>`, coerenti con `loading="eager"/"lazy"` già presente.
- Fix bug minore non collegato: `src/pages/portfolio/[category]/index.astro` aveva l'attributo `quality={90}` duplicato sul componente `Image` della griglia categoria.

**Perché:** le altre immagini del sito (portfolio, about, video/utility) passano già correttamente da `<Image>` di `astro:assets` (ottimizzate/responsive), solo la hero slideshow usava l'import grezzo perché passa i `src` come stringhe a un componente React (`HeroSlideshow.tsx`), bypassando l'ottimizzazione automatica.

**Come si applica:** se in futuro si aggiungono altre immagini passate a componenti React (non `.astro`) come stringa `src`, usare sempre `getImage()` in frontmatter per generare la versione ottimizzata invece di passare `import.src` direttamente — l'ottimizzazione di Astro non è automatica fuori dal componente `<Image>`.

## 2026-08-08 — Aggiunto switch lingua IT/EN (bandierine) a tutto il sito

L'utente ha chiesto di poter switchare l'intero sito tra italiano e inglese con due bandierine (Italia/UK), termini tecnici (stacking, plate solve, ecc.) sempre in inglese anche nella versione italiana. Discusse due opzioni con l'utente ([AskUserQuestion](../CLAUDE.md)): routing separato `/it/` `/en/` (stile i18n nativo di Astro) vs toggle client-side senza cambio URL. L'utente ha scelto il **toggle client-side**, con **italiano come lingua di default**.

Implementazione (nessun URL dedicato, stessa pagina per entrambe le lingue):
- `src/components/LangSwitcher.astro`: due bottoni con bandiera (SVG inline IT/UK), fissi in alto a destra accanto all'hamburger menu. Al click, salva la lingua scelta in `localStorage` (`site-lang`, default `it`) e imposta `data-lang` su `<html>`.
- `src/layouts/Layout.astro`: script bloccante `is:inline` in cima all'head che legge `localStorage` e imposta `data-lang`/`lang` su `<html>` **prima del paint**, per evitare un flash della lingua sbagliata. `<LangSwitcher />` incluso qui, quindi presente su ogni pagina.
- `src/styles/global.css`: entrambe le lingue sono sempre nel DOM; le regole `[data-lang-en]`/`[data-lang-it]` (mostra/nascondi in base a `data-lang` su `:root`) fanno il toggle via CSS puro, default italiano se l'attributo non è ancora impostato (no-JS).
- `src/components/T.astro`: componente helper per testo inline breve (rende due `<span data-lang-en>`/`<span data-lang-it>`), usato raramente — nella maggior parte delle pagine si è preferito scrivere `<span data-lang-en>…</span><span data-lang-it>…</span>` direttamente inline per restare coerenti con lo stile JSX-like già in uso nel progetto.
- Contenuti lunghi (bio, schede portfolio, pagine `/utility/*`, privacy): duplicati come blocco `<div data-lang-en>…</div><div data-lang-it>…</div>` invece di span, per non spezzare paragrafi/markdown.
- Attributi non duplicabili nel DOM (title `<title>`, `<meta name="description">`, `placeholder`, `aria-label`) via attributo `data-tr='{"attr":"...","en":"...","it":"..."}'`, letto dallo script di `LangSwitcher.astro` che aggiorna l'attributo al cambio lingua.
- Contenuto markdown delle content collection: aggiunte due nuove collection in `src/content.config.ts`, **`portfolioIt`** (`src/content/portfolio-it/*.md`, stesso id/filename di ogni file in `src/content/portfolio/`) e **`aboutIt`** (`src/content/about-it/bio.md`) — file markdown italiani paralleli, non campi frontmatter aggiuntivi, così il testo italiano resta vero markdown (bold, link, liste) invece di stringhe piatte. `src/pages/portfolio/[category]/[object].astro` e `src/pages/about/index.astro` fanno `render()` su entrambe le entry e mostrano il blocco giusto via `data-lang-en`/`data-lang-it`.
- `src/data/site.ts`: `mainNav` ha `labelIt` accanto a `label`, `categories` ha `titleIt`/`blurbIt` accanto a `title`/`blurb`, `site` ha `descriptionIt`.
- Termini tecnici (stacking, plate solve, blending mode, ecc.) lasciati non tradotti nel testo italiano su richiesta esplicita dell'utente.

**Perché:** l'utente vuole che il sito sia fruibile sia in italiano che in inglese, senza dover mantenere pagine duplicate per lingua (routing `/it/`/`/en/` scartato per semplicità di manutenzione su un sito di queste dimensioni).

**Come si applica:** per nuove pagine o nuovo testo statico, seguire lo stesso pattern — span `data-lang-en`/`data-lang-it` per testo breve inline, div per blocchi lunghi/paragrafi, `data-tr` per attributi HTML. **Superato per il markdown lungo (portfolio/about) dalla voce successiva**, che consolida le collection gemelle `*It` in un unico file per entry.

## 2026-08-08 — Consolidate collection `portfolioIt`/`aboutIt` in un unico file bilingue per entry

Le collection gemelle `portfolioIt` (38 file in `src/content/portfolio-it/`) e `aboutIt` (`src/content/about-it/bio.md`), introdotte nella voce precedente, duplicavano un intero file `.md` per ogni entry solo per il testo italiano — stesso `id`/filename, frontmatter quasi identico, spesso senza `image` (con fallback all'inglese) o altri campi. Questo aveva già causato un bug reale: `portfolio-it/andromeda.md` aveva `image` copiato per errore da `b150.jpg` invece di ereditare quello inglese.

L'utente ha chiesto di gestire le due lingue con **un solo file per entry** invece di due file in cartelle separate, con una sola immagine condivisa.

Cambiato:
- `src/content.config.ts`: rimosse le collection `portfolioIt`/`aboutIt`. `portfolio` e `about` hanno ora un campo frontmatter `bodyIt: z.string()` (markdown italiano grezzo, YAML block scalar `|`) accanto al body markdown esistente (che resta il testo inglese, invariato). `about` ha anche `titleIt: z.string()` (i titoli portfolio non necessitavano un `titleIt`: verificato che sono identici in tutte le 38 coppie, es. "M31 Andromeda").
- Aggiunta dipendenza `marked` (package.json) per convertire `bodyIt` (stringa markdown) in HTML a build time, dato che `render()` di Astro funziona solo su entry di collection vere, non su stringhe di frontmatter.
- `src/pages/portfolio/[category]/[object].astro` e `src/pages/about/index.astro`: non fanno più `getEntry('portfolioIt'/'aboutIt', ...)`; usano `entry.data.image` direttamente (una sola immagine, niente più `entryIt?.data.image ?? entry.data.image`) e `<div data-lang-it set:html={await marked.parse(entry.data.bodyIt)} />` al posto del secondo `<Content />`.
- 38 file `src/content/portfolio/*.md` migrati con uno script Node una tantum (merge frontmatter + `bodyIt` indentato), poi cancellate le cartelle `portfolio-it/` e `about-it/`.
- Build verificata (`npx astro build`, 58 pagine, nessun errore); controllato l'HTML generato per `/portfolio/galaxies/andromeda/` (ora usa `andromeda.jpg` per entrambe le lingue, bug corretto) e `/about/`.

**Perché:** meno file da mantenere in sync (1 invece di 2 per entry), impossibile che le due lingue divergano su campi condivisi come `image`/`category`/`astrobinLink` (la classe di bug vista su andromeda diventa strutturalmente impossibile), workflow di editing più snello come richiesto dall'utente.

**Come si applica:** per aggiungere/modificare una scheda portfolio o la bio, editare **un solo file** (`src/content/portfolio/<slug>.md` o `src/content/about/bio.md`): body markdown = inglese, campo frontmatter `bodyIt: |` (indentato di 2 spazi) = italiano. Se in futuro serve un altro campo che può differire per lingua (oltre a `bodyIt`/`titleIt`), aggiungerlo come `<campo>It` nello schema Zod della collection, non come collection/file separato.

## 2026-08-09 — Dati di acquisizione AstroBin importati via scraping Node+Playwright (non API)

L'utente voleva mostrare sul sito i dati di acquisizione AstroBin (ottiche, filtri, tempo di integrazione) di ogni foto del portfolio, partendo dal campo `astrobinLink` già presente nel frontmatter di ogni `.md`. Piano iniziale: script Python con la API ufficiale AstroBin v2 (key/secret).

Bloccato su entrambi i fronti:
- Le richieste di nuove API key AstroBin sono **chiuse** ("Le richieste di chiavi API sono attualmente chiuse", screenshot utente 2026-08-09); l'abbonamento AstroBin dell'utente è anche scaduto.
- Python **non è installato** su questa macchina (solo lo stub del Microsoft Store) — Node invece sì, ed è già lo stack del progetto.
- Scraping HTTP diretto (curl/requests) delle pagine AstroBin è bloccato a livello Cloudflare/CloudFront con 403 consistente, anche spoofando lo user-agent: è una SPA Angular con bot-detection sul fingerprint TLS/JS, non una pagina statica.

Soluzione trovata: **Playwright (Node) headless**, che ha un fingerprint da browser reale e passa il blocco. Analizzando le richieste di rete della pagina renderizzata è emerso che l'app AstroBin stessa chiama, senza autenticazione, `https://app.astrobin.com/api/v2/images/image/?hash=<id>` — endpoint pubblico con tutti i dati (equipaggiamento v2, filtri, sessioni `deepSkyAcquisitions` con filtro/numero pose/durata/gain/data, software). Nessuna API key necessaria.

Implementato:
- [scripts/fetch-astrobin.mjs](../scripts/fetch-astrobin.mjs): legge `astrobinLink` da ogni `.md` in `src/content/portfolio/**`, apre la pagina in Chromium headless, intercetta quella risposta API, salva una versione semplificata in `src/data/astrobin.json` (indicizzato per slug del file, es. `"cone": {...}`). Ha cache (salta slug già presenti) — `--force` per rifare tutto, `--slug <nome>` per una singola entry di test.
- `playwright` aggiunto come devDependency (Chromium già scaricato con `npx playwright install chromium`), script npm `astrobin:fetch`.
- `src/pages/portfolio/[category]/[object].astro`: nuova sezione "Equipment & Acquisition"/"Attrezzatura e acquisizione" (bilingue, stesso pattern `data-lang-en`/`data-lang-it` del resto del sito) che legge `astrobin.json` per slug e mostra telescopio/camera/montatura/integrazione totale/filtri/tabella sessioni. Il bottone esterno verso AstroBin resta sotto, rinominato "View on AstroBin"/"Vedi su AstroBin" (prima diceva "Equipment and Acquisition Details", fuorviante ora che i dettagli sono sulla pagina stessa).

**Perché:** dati di acquisizione mostrati direttamente sul sito invece che solo tramite link esterno ad AstroBin, aggirando i due blocchi (API chiusa, niente Python) con lo strumento già disponibile nel progetto (Node) e un endpoint pubblico non documentato ma stabile perché è quello che il sito stesso usa.

**Come si applica:** per aggiungere una nuova foto al portfolio, dopo aver scritto il `.md` con `astrobinLink`, lanciare `npm run astrobin:fetch` per popolare la entry mancante in `astrobin.json` — la pagina di dettaglio la userà automaticamente se presente (sezione non mostrata se assente). Se AstroBin cambia la struttura di quella risposta API o la blocca, verificare prima con `node scripts/fetch-astrobin.mjs --slug <nome>` (un solo tentativo, log chiaro) prima di rilanciare su tutte le 38 entry. **Superato dalla voce successiva per l'aggiornamento periodico automatico.**

## 2026-08-09 — Sincronizzazione AstroBin resa periodica/automatica (cache con scadenza a 24h)

L'utente ha notato che i dati di Andromeda mostrati sul sito erano vecchi nonostante li avesse aggiornati su AstroBin, e ha chiesto di automatizzare la cosa per avere sempre dati aggiornati.

Scoperto (non introdotto in questa sessione, gia' presente da uno step precedente non documentato qui prima d'ora): `astro.config.mjs` ha gia' un'integrazione `astrobinSync` che rilancia `node scripts/fetch-astrobin.mjs` a ogni `astro:config:setup`, cioe' a ogni avvio di `astro dev`/`astro build` — quindi la sincronizzazione gira gia' in automatico. Il problema era la funzione `isStale()` in `fetch-astrobin.mjs`: considerava un'entry valida per sempre finche' `astrobinLink` nel `.md` non cambiava, quindi una modifica fatta solo lato AstroBin (stesso link, stessa foto) non veniva mai ripresa.

Cambiato in `scripts/fetch-astrobin.mjs`:
- Ogni entry salvata porta ora un campo `_fetchedAt` (timestamp ISO).
- `isStale(cachedEntry, url)` ritorna `true` anche se sono passate piu' di `CACHE_TTL_MS` (costante, 24 ore) dall'ultimo fetch, oltre ai casi "entry assente" e "link cambiato" di prima. Entry vecchie senza `_fetchedAt` (formato pre-esistente) sono trattate come scadute.
- Nessuna modifica alla logica di `main()`/all'integrazione in `astro.config.mjs`: bastava cambiare la condizione di scadenza, il meccanismo di rilancio automatico esisteva gia'.

Eseguito subito `node scripts/fetch-astrobin.mjs --slug andromeda --force` per lo caso segnalato, poi `node scripts/fetch-astrobin.mjs` (senza force) su tutte le 38 entry per portarle tutte al nuovo formato con `_fetchedAt` (le vecchie, senza timestamp, sono state trattate come scadute e riscaricate automaticamente). Verificato che tutte le 38 entry abbiano `_fetchedAt` e che `npx astro build` completi senza errori con la nuova cache.

**Perche':** l'utente vuole dati sempre aggiornati senza dover ricordarsi di rilanciare comandi manuali ogni volta che modifica qualcosa su AstroBin. Una cache a scadenza temporale (invece che solo per link) risolve il problema restando dentro l'infrastruttura gia' esistente (nessun bisogno di GitHub Actions o servizi esterni): la sincronizzazione gira comunque a ogni `dev`/`build`, ora si limita a controllare anche l'eta' del dato.

**Come si applica:** i dati su AstroBin modificati senza cambiare il link arrivano sul sito entro 24 ore dal prossimo avvio di `astro dev` o build (non in tempo reale: serve che qualcuno avvii dev/build dopo la scadenza). Per un aggiornamento immediato di una singola foto, resta `node scripts/fetch-astrobin.mjs --slug <nome> --force`. Se in futuro serve una freschezza diversa da 24h, cambiare solo la costante `CACHE_TTL_MS` in cima a `scripts/fetch-astrobin.mjs`. Se il sito viene deployato da un ambiente CI/hosting che non esegue mai `astro dev`/`build` in prima persona con Playwright disponibile (es. build fatta altrove o Chromium non installato), questa automazione non si attiva li' — finora il sito viene buildato/deployato da questa macchina locale, quindi non e' un problema noto, ma va verificato se cambia il flusso di deploy in futuro.

**Attenzione operativa:** durante il debug di questa feature, creare/cancellare file temporanei nella root del progetto (es. HTML/JSON di scratch) ha fatto riavviare più volte il dev server Astro (Vite osserva tutta la root), lasciando la tab del browser dell'utente con un client stantio e navigazione portfolio apparentemente rotta (falso allarme, risolto con un refresh). In futuro, i file di scratch per debug vanno nella cartella scratchpad di sessione, non nella root del repo.

**Aggiornamento 2026-08-09 — campi aggiuntivi:** l'utente ha chiesto anche luogo di ripresa, data, scala di Bortle, emisfero, coordinate celesti, campionamento, raggio del campo, strumentazione di guida, accessori, software (tutti presenti nello stesso endpoint AstroBin, solo non ancora estratti/mostrati). Aggiunto a `simplify()` in `fetch-astrobin.mjs`: `guidingTelescopes`/`guidingCameras` (da `guidingTelescopes2`/`guidingCameras2`), `locations` (da `locationObjects`: city/state/country/lat-lon con `latSide` N/S), `solution` (da `raw.solution`: `ra`/`dec` in gradi, `pixscale` arcsec/px, `radiusDeg`). In `[object].astro`: helper `formatRA`/`formatDec` (conversione gradi → hh:mm:ss / ±dd°mm′ss″), `formatLocation` (via `Intl.DisplayNames` per il nome nazione bilingue, niente hardcoding), `mostCommonBortle` (moda tra le sessioni di acquisizione, dato che il Bortle è per-sessione non per-immagine). Emisfero derivato da `latSide` della location (non dal segno della declinazione): rappresenta l'emisfero terrestre del sito di ripresa, non quello celeste dell'oggetto. Location/emisfero non vengono mostrati se assenti su AstroBin per quella foto (es. `cone` non ha location impostata) — nessun campo vuoto in UI. Verificato visivamente con screenshot Playwright sia su una foto con location (`rosettahoo`) sia su una senza (`cone`).

## 2026-08-09 — Installata skill esterna "UI/UX Pro Max" per Claude Code

L'utente ha chiesto di installare la skill al link `https://ui-ux-pro-max-skill.nextlevelbuilder.io/`, poi (dopo che ho segnalato di non poter installare da URL esterni arbitrari non verificati) indicato il repository sorgente su GitHub: `github.com/nextlevelbuilder/ui-ux-pro-max-skill`. Verificato prima di eseguire: pacchetto npm reale e pubblico (`ui-ux-pro-max-cli`, MIT license, maintainer NextLevelBuilder/mrgoonie), non un dominio Anthropic — installazione confermata esplicitamente dall'utente prima di eseguire `npx` (che scarica ed esegue codice di terze parti).

Eseguito `npx ui-ux-pro-max-cli init --ai claude` nella root del repo. Ha generato `.claude/skills/` con 7 sotto-skill: `ui-ux-pro-max` (motore di reasoning con 84 stili UI, 192 palette colori, 74 font pairing, 161 regole per settore), `design`, `design-system`, `brand`, `ui-styling`, `banner-design`, `slides` (ognuna con proprio `SKILL.md`, dati/script/template).

**Perché:** l'utente vuole un supporto più strutturato per decisioni di design UI/UX (palette, stili, font, design system) nel lavoro su questo sito.

**Come si applica:** le nuove skill sono disponibili come qualsiasi altra skill di sistema (via `Skill` tool) dopo il riavvio della sessione — usarle quando si lavora su aspetti visivi/di design del sito (es. ridisegno sezioni, scelta palette colori, tipografia). Se in futuro serve aggiornare la skill, rieseguire lo stesso comando `npx ui-ux-pro-max-cli init --ai claude` (verificare prima la versione più recente su npm).

## 2026-08-09 — Ridisegnata la sezione "Equipment & Acquisition" (era percepita come "AI slop")

L'utente ha segnalato che la sezione dati di acquisizione (screenshot) sembrava generica/da-IA: una griglia `<dl>` piatta a 2 colonne, tutte le coppie etichetta/valore con lo stesso peso visivo, nessuna icona, nessuna gerarchia. Provata la skill `ui-ux-pro-max` (CLI Python non disponibile su questa macchina — Python non installato, vedi nota sotto — quindi usata solo la Quick Reference del `SKILL.md`, non il tool di ricerca).

Ridisegnata in `src/pages/portfolio/[category]/[object].astro` con gerarchia editoriale "pannello strumenti":
1. Header con eyebrow + stat in evidenza (integrazione totale, testo grande accent-colored con icona orologio) a destra.
2. Riga "equipaggiamento primario": 3 card (Telescopio/Camera/Montatura) con icona + valore in `font-medium`, separate da `divide-x`.
3. `<dl>` compatta per i dati secondari (guida, luogo, Bortle, emisfero, coordinate, campionamento, raggio campo), ogni `dt` con una piccola icona inline (16px) prima dell'etichetta.
4. Tag filtri/accessori/software: stesso stile pill di prima ma con icona di gruppo e hover `border-accent/40`.
5. Tabella sessioni: intestazione con icona calendario, zebra striping (`even:bg-white/[0.02]`), numeri in `tabular-nums`.

Tutte le icone sono SVG inline disegnate a mano (nessuna libreria icone installata nel progetto), stroke-width 1.75, viewBox 24×24, stile coerente con le frecce già usate in `.media-card-label` (stroke-width 2, `fill="none" stroke="currentColor"`).

Verificato con screenshot Playwright (script temporaneo in scratchpad, non committato): pagina con tutti i campi (`rosettahoo`), pagina con campi parziali/senza location (`cone`), viewport mobile 390px, ed entrambe le lingue IT/EN — nessun problema di layout, i blocchi condizionali (`hasPrimaryEquipment`/`hasSecondaryDetails`) nascondono correttamente le sezioni vuote senza lasciare buchi.

**Perché:** l'utente vuole che questa sezione (presente su tutte le ~38 schede portfolio) comunichi i dati come un "pannello di controllo" osservativo invece che come una lista burocratica — dando peso visivo maggiore ai dati che contano di più (tempo di integrazione, ottica usata) e trattando i dati tecnici densi (RA/Dec, pixel scale) come secondari ma leggibili.

**Come si applica:** questo è ora il pattern per la sezione acquisizione; se in futuro cambiano i campi da mostrare (nuovi dati AstroBin), seguire la stessa gerarchia a 3 livelli (stat in evidenza → equipaggiamento primario a card → dettagli secondari in `dl` compatta con icone) invece di tornare a una griglia piatta. Su questa macchina Python non è installato: la CLI di ricerca di `ui-ux-pro-max` non è utilizzabile, va usata solo la Quick Reference statica nel `SKILL.md` finché Python non viene installato dall'utente.

**Aggiornamento 2026-08-09 — Il ridisegno "card + icone" è stato bocciato ("è peggio di prima"), sostituito con un readout FITS/console.** L'utente ha chiesto esplicitamente di cercare un'altra skill online che facesse "qualcosa di totalmente diverso". Installata la skill ufficiale Anthropic **`frontend-design`** (`npx skills add https://github.com/anthropics/skills --skill frontend-design`, verificata: Safe/0 alert/Low Risk su skills.sh) in `.agents/skills/frontend-design` (symlink in `.claude/skills/`). A differenza di `ui-ux-pro-max` (motore di pattern/regole basato su ricerca CLI), questa skill è un metodo di design: chiede di ancorare le scelte al soggetto reale prima di scrivere codice, evitare i 3 look-default dell'"AI slop" (crema+serif+terracotta; nero+un accento acido; broadsheet a colonne), e prendere un solo rischio estetico giustificato invece di decorare uniformemente.

Applicata al pannello: invece della griglia di card con icone (bocciata), il pannello ora imita un **readout FITS/console da telescopio** — il vero formato con cui ogni file astronomico salva i metadati (`CHIAVE = valore / commento`), reso come log di strumentazione reale (monospace, ambra su nero, un solo elemento in movimento: cursore `_` lampeggiante a fine log). Le chiavi (`TELESCOP`, `INSTRUME`, `MOUNT`, `SITE`, `OBJCTRD`, ecc.) restano sempre in inglese in entrambe le lingue — non tradotte per scelta thematic-coerente (i veri header FITS sono sempre in ASCII/inglese) e per riuso della convenzione già esistente sul sito di lasciare il gergo tecnico non tradotto; il commento dopo `/` porta la label bilingue umana.

Implementazione: aggiunto font `@fontsource/space-mono` (import diretto in `[object].astro`, non in `Layout.astro`, per tenerlo scoped a questa sezione), CSS scoped nel componente Astro (non in `global.css`, essendo un trattamento bespoke di un solo blocco) con: scanline di sfondo (`repeating-linear-gradient` opacità ~3.5%), bordo/testo ambra riusando i token esistenti `--color-accent`/`--color-accent-dim`, tabella sessioni come ledger (niente zebra striping, righe con hairline), indicatore "PLATE SOLVE OK"/"LOG" in alto a destra condizionato alla presenza di `acquisition.solution`. Icone SVG rimosse del tutto (erano il pattern generico bocciato).

Verificato con build (`npx astro build`, 58 pagine senza errori) e screenshot Playwright: dati completi (`rosettahoo`), dati parziali senza location (`cone`, verificato che le righe SITE/HEMISPH spariscono senza lasciare buchi), viewport mobile 390px (i valori vanno a capo restando leggibili), entrambe le lingue.

**Perché:** la prima iterazione (card con icone, stat in evidenza, pillole) restava comunque nel linguaggio visivo "dashboard SaaS generica" nonostante la gerarchia migliorata — esattamente il tipo di risultato che la skill `frontend-design` descrive come "template answer" (grande numero + card a griglia + icone). L'utente ha riconosciuto il problema e chiesto una direzione radicalmente diversa, radicata nel soggetto reale (astrofotografia) invece che nei pattern UI generici.

**Come si applica:** per il resto del sito (che non ha ricevuto questo trattamento) resta valido lo stile "dashboard" esistente — questo pannello è un'eccezione intenzionale con la sua identità visiva, non un nuovo standard da propagare altrove senza una richiesta esplicita. Se in futuro serve un'altra sezione con un'identità visiva distintiva/non-template, usare la skill `frontend-design` (metodo: ancorare al soggetto, 2 passate con auto-critica, un solo rischio estetico) invece di `ui-ux-pro-max` (più adatta a UI convenzionali basate su pattern/palette pre-validati).

**Aggiornamento 2026-08-09 — Fix: la chiave del ledger mostrava il codice FITS invece del titolo leggibile.** Nella prima versione ogni riga era `TOTEXP = 29h 55m / Integrazione totale`: il codice tecnico (`TOTEXP`) era la chiave (`dt`, in evidenza) e il titolo umano bilingue era relegato a commento in coda, a malapena visibile. L'utente ha segnalato con uno screenshot annotato che non aveva senso mostrare il nome-variabile quando si aveva già il titolo leggibile. Invertito: ora `dt` contiene il titolo bilingue (`data-lang-en`/`data-lang-it`, reso maiuscolo via CSS `text-transform: uppercase` invece di scriverlo già in caps nel markup) e `dd` contiene solo il valore — niente più `/ commento` in coda. Il codice FITS originale (TOTEXP, TELESCOP, OBJCTRD, ecc.) non è sparito: resta come attributo `title` sul `dt`, quindi visibile come tooltip nativo del browser per chi lo cerca, senza intasare la lettura. `dt` allargato a `min-width: 11rem` (da 6.5rem) per ospitare le etichette più lunghe.

**Perché:** priorità di lettura sbagliata nella v1 — l'occhio cade prima sulla chiave (posizione/peso visivo maggiore), che deve essere l'informazione utile (cosa rappresenta il dato), non il gergo interno.

**Come si applica:** se si aggiungono altre righe al ledger in futuro, la struttura è sempre `<dt title="CODICE_FITS"><span data-lang-en>Titolo</span><span data-lang-it>Titolo IT</span></dt><dd><span class="ledger-val">valore</span></dd>` — mai il codice tecnico come contenuto visibile di `dt`.

**Aggiornamento 2026-08-09 — Rimossi altri residui "da terminale finto": segno `=`, apici, valori multipli su una riga.** L'utente ha segnalato con uno screenshot annotato (cerchi verdi) tre cose: il segno `=` dopo ogni etichetta era un simbolo inutile ripetuto su ogni riga; gli apici singoli (`'...'`) attorno ai valori con più elementi (es. filtri, accessori, software) erano di intralcio; e il prompt/cursore finale (`> _`) in fondo al pannello era un altro simbolo decorativo senza scopo.

Rimosso: `dt::after { content: ' =' }` in CSS; il blocco `.console-end`/`.console-caret` (comprese le keyframe `console-blink` e la regola `prefers-reduced-motion` collegata) e il markup corrispondente a fine pannello. Rimossi gli apici da tutti i valori (erano un vezzo "stile file dati" che il codice FITS nel tooltip rende già superfluo). Per i campi che possono avere più valori (telescopi, camere, montature, guida, filtri, accessori, software) il valore non è più `array.join(', ')` su una riga che va a capo in modo disordinato, ma uno stack verticale: `<dd class="ledger-stack">{array.map((v) => <span class="ledger-val">{v}</span>)}</dd>`, con CSS `.ledger-row dd.ledger-stack { flex-direction: column; align-items: flex-start; gap: 0.2rem }` (di default `dd` resta `flex-direction: row` per i valori singoli come luogo/coordinate/emisfero).

**Perché:** ordine visivo — con più filtri o più voci software, il testo su una riga sola andava a capo in modo che mischiava valori diversi senza confini chiari; impilarli uno per riga rende evidente dove finisce un elemento e comincia il successivo, senza bisogno di virgole o apici come separatori.

**Come si applica:** per nuovi campi multi-valore nel ledger, usare sempre il pattern `ledger-stack` (un `<span class="ledger-val">` per elemento) invece di `join(', ')`; per campi a valore singolo, il semplice `<span class="ledger-val">valore</span>` dentro `<dd>` senza la classe `ledger-stack`. Non reintrodurre `=`, apici o elementi puramente decorativi (prompt/cursore) senza una richiesta esplicita — l'utente ha già respinto quel tipo di fronzolo due volte in questa sezione.

**Aggiornamento 2026-08-09 — Rimossi anche il prompt `>` e l'indicatore "PLATE SOLVED", titolo ingrandito.** Ultimo giro di pulizia sugli stessi fronzoli decorativi: l'utente ha cerchiato in verde il simbolo `>` prima del titolo e il tag di stato "PLATE SOLVED"/"LOG" (pallino + testo) in alto a destra nella topbar, chiedendo di eliminarli ed ingrandire il titolo sottolineato rendendolo ben allineato ("incolonnato").

Rimossi da `[object].astro`: `<span class="console-prompt">` e l'intero blocco `<span class="console-status">` (con la logica condizionale plate-solved/log). La topbar ora contiene solo il titolo bilingue. CSS: tolte `.console-prompt`/`.console-status`/`.console-status-dot` (non più referenziate); `.console-topbar` semplificata (non serve più `display:flex`/`justify-content:space-between` con un solo elemento); `.console-title` portato da `font-size: 0.75rem` a `1.125rem`, con `text-transform: uppercase` esplicito in CSS (il testo nel markup è stato riscritto in title-case leggibile — "Equipment & Acquisition Log" — invece che già in maiuscolo, la trasformazione la fa il CSS) e `letter-spacing` ridotto da `0.08em` a `0.04em` per restare leggibile alla dimensione più grande senza sovrapporsi su schermi stretti.

**Perché:** l'indicatore "PLATE SOLVED" era un vezzo estetico (nod al plate-solving) senza reale utilità per chi legge la pagina, e il prompt `>` duplicava un'idea di "terminale" già resa a sufficienza da font monospace/colori/scanline — l'utente lo ha giudicato rumore visivo, coerente con le due richieste di pulizia precedenti in questa stessa sezione (rimozione di `=`, apici, cursore lampeggiante).

**Come si applica:** la topbar del pannello ora è solo titolo, niente indicatori di stato decorativi. Se in futuro serve segnalare qualcosa lì (es. un vero stato, non un abbellimento), valutare prima se è informazione utile per l'utente finale o solo un'idea "da terminale" — questa sezione ha già dimostrato una bassa tolleranza per il secondo caso.

## 2026-08-10 — Fix: build produceva ~350MB di foto originali non ottimizzate, mai referenziate da nessuna pagina

L'utente ha segnalato, dopo il deploy su Altervista, che le immagini di copertina di home/portfolio non si caricavano all'apertura e che il click per aprire le sezioni era lento. Indagando `dist/` (rigenerata con `npx astro build`): la cartella `dist/_astro` pesava **446MB**, di cui **349MB** erano 34 file `.jpg` senza suffisso dimensionale (es. `andromeda.DxIhihOR.jpg`, 14MB) — confrontati byte-per-byte con `src/content/portfolio/**/*.jpg`, erano le foto **originali intatte**, non passate da nessuna ottimizzazione. Verificato con `grep` su tutto `dist/` (html/css/js/xml) che **nessuna pagina le referenzia**: ogni pagina usa solo `<Image>`/`getImage()` (derivati `.webp` con suffisso hash, quelli sì referenziati). C'erano anche due file `dist/courses.zip`/`dist/dist.zip` da ~515MB ciascuno, artefatti manuali di un tentativo di upload precedente, dentro la cartella che veniva ripacchettata per il deploy.

Causa: lo schema Zod della collection `portfolio` in `content.config.ts` usa l'helper `image()` (vedi [voce sopra su image() e content collections](#)) — quell'import fa sì che Vite copi anche l'asset originale grezzo in `dist/_astro` con hash, in aggiunta (non al posto) delle derivate ottimizzate generate da `getImage`/`<Image>`. È un side-effect del meccanismo di import di Vite, non un bug nel codice del sito: succede per ogni immagine referenziata via `image()` nello schema di una content collection, a prescindere da come poi la si usa nelle pagine.

Fix: [scripts/prune-dist-assets.mjs](../scripts/prune-dist-assets.mjs), lanciato automaticamente come script `postbuild` (in `package.json`, dopo `astro build`). Cammina tutto `dist/`, legge il testo di ogni file `.html`/`.css`/`.js`/`.xml`/`.json`/`.txt`, e cancella ogni file sotto `dist/_astro` il cui nome non compare in nessuno di quei testi — controllando sia il nome letterale sia la sua forma URL-encoded (`encodeURIComponent`), perché nomi con spazi/parentesi (es. `Video Project (1).mp4`) compaiono con `%20` negli attributi `src`/`href` e un primo tentativo senza questo controllo cancellava per errore anche file legittimi come il video hero dei corsi. Risultato dopo fix: `dist/` passa da 446MB (solo `_astro`, prima ancora di contare i due zip da 515MB) a **118MB totali**, senza nessun file mancante (verificato che ogni file rimasto in `_astro` compaia in almeno una pagina buildata).

**Perché:** un hosting condiviso gratuito come Altervista ha quota di spazio/banda limitata; un deploy reale di ~1GB+ (446MB di `_astro` più i due zip da 515MB dentro la stessa cartella) spiega sia upload lenti/incompleti sia immagini che non si caricano (probabile troncamento/quota superata durante l'upload), a prescindere da qualunque ottimizzazione lato codice già presente (vedi [voce sul fix latenza hero](#2026-08-08--fix-latenza-immagini-hero-allapertura-del-sito), che aveva già risolto lo stesso problema per le hero slide ma non toccava questo meccanismo separato dei content-collection asset).

**Come si applica:** `npm run build` ora pulisce automaticamente `dist/` — non serve più nessun passaggio manuale. Per il prossimo deploy su Altervista: ricostruire con `npm run build` e caricare la `dist/` risultante (circa 100-120MB, non ~1GB); gli eventuali `dist.zip`/`courses.zip` vecchi lasciati sul server andrebbero rimossi manualmente lato Altervista per liberare quota, dato che questa sessione ha solo pulito la copia locale. Se in futuro si aggiunge un altro campo `image()` a una content collection (o una nuova collection con foto), lo stesso side-effect si ripresenterà per quei file — lo script di pruning lo gestisce già automaticamente, nessuna azione aggiuntiva richiesta.

## 2026-08-10 — Immagini che restano "rotte" sul sito live finché non si ricarica la pagina: instabilità di rete di Altervista, non un bug del sito

Dopo il fix precedente (pruning di `dist/`), l'utente ha ancora visto le copertine del portfolio nere/vuote al primo caricamento del sito live (`valerioavitabile.altervista.org`), che tornavano visibili solo dopo un refresh manuale (F5). Diagnosticato passo per passo:
- Tutte le URL delle immagini nell'HTML live rispondono 200 via `curl` (79 asset controllati, incluso lo script `ScrollReveal...js`, verificato byte-per-byte identico alla build locale) — quindi non è un problema di file mancanti/deploy incompleto residuo.
- L'animazione "iris reveal" delle card portfolio (`.iris-reveal`, `clip-path: circle(0%)` finché JS non aggiunge `.is-visible` via `IntersectionObserver`, vedi [ScrollReveal.astro](../src/components/ScrollReveal.astro)) è stata verificata **corretta** con un test Playwright reale contro il sito live: scrollando la gallery in viewport, tutte le 8 card si aprono regolarmente (`clip-path: circle(75%)`) — quindi non è un bug di logica JS.
- Lo stesso test Playwright, lasciato girare senza filtri, ha però catturato più richieste immagine fallite con `net::ERR_HTTP2_PROTOCOL_ERROR` / `net::ERR_CONNECTION_RESET` **al primo carico**, e un intero `page.reload()` è arrivato a fallire del tutto con `net::ERR_HTTP2_PROTOCOL_ERROR` sulla connessione HTTP/2 verso Altervista. Questo è instabilità/throttling lato server (piano condiviso/gratuito) sotto il carico di richieste concorrenti (immagini + font + script tutti insieme), non qualcosa di riproducibile deterministicamente né dipendente dal codice — un refresh "funziona" perché molte risorse arrivano già dalla cache del browser, riducendo le connessioni simultanee che il server deve reggere.
- Nota a margine emersa dallo stesso test: `BackgroundMusic.astro` carica un file audio da `cdn.jsdelivr.net/gh/valerioavitabile-cyber/mysite/...` (repo/CDN esterno) che fallisce con `net::ERR_ABORTED` — non ancora indagato, comportamento preesistente non toccato in questa sessione.

Fix applicato (mitigazione, non risoluzione della causa che è lato host): script `is:inline` in [Layout.astro](../src/layouts/Layout.astro), listener `error` in capture phase su `document` (gli eventi `error` delle `<img>` non fanno bubbling) che riprova a caricare qualsiasi immagine fallita fino a 3 volte con backoff (800ms, 1600ms, 2400ms) riassegnando `img.src` allo stesso URL, tracking dei tentativi per-immagine via `WeakMap`.

**Perché:** non è possibile risolvere l'instabilità di rete del server Altervista dal codice del sito; una `<img>` la cui richiesta viene droppata a metà (HTTP/2 protocol error) resta rotta per sempre senza un retry esplicito — il browser non ritenta da solo. Un retry con backoff breve copre la stragrande maggioranza dei casi transitori senza serie ritentate infinite o carico aggiuntivo percepibile.

**Come si applica:** questo è un cerotto applicato a tutte le `<img>` del sito (script globale in `Layout.astro`, non specifico al portfolio). Se il problema persiste nonostante il retry, la causa è quasi certamente il piano Altervista stesso (limite di banda/connessioni concorrenti) — le opzioni sarebbero ridurre il numero di immagini caricate in parallelo nella prima viewport, o valutare un piano Altervista a pagamento / hosting diverso, non ulteriori modifiche al codice applicativo.

## 2026-08-10 — Setup deploy su GitHub Pages (in parallelo ad Altervista)

L'utente ha chiesto di predisporre il deploy su GitHub Pages. Per ora usa l'URL github.io di default (`valerioavitabile-cyber.github.io/Astrofotografia`), non ancora il dominio personalizzato — passerà al dominio custom in un secondo momento.

Fatto:
- [astro.config.mjs](../astro.config.mjs): `site` cambiato a `https://valerioavitabile-cyber.github.io` e aggiunto `base: '/Astrofotografia'` (necessario perché GitHub Pages di default serve il sito da un sottopercorso col nome del repo, non dalla root — senza `base` tutti i link/asset assoluti sarebbero rotti). **Nota**: questo `site`/`base` è in conflitto con quello usato per Altervista (dominio custom, `base` implicito `/`) — i due deploy non possono condividere la stessa build finché si usa github.io; quando l'utente passerà al dominio personalizzato su GitHub Pages, `site` va rimesso al dominio custom e `base` va rimosso (o messo a `/`), aggiungendo un file `public/CNAME` con dentro il dominio.
- [.github/workflows/deploy.yml](../.github/workflows/deploy.yml): workflow che builda ed esegue il deploy su push a `main` (via `actions/configure-pages` + `upload-pages-artifact` + `deploy-pages`, il meccanismo ufficiale "Deploy from GitHub Actions", non il branch `gh-pages`). Include uno step `npx playwright install --with-deps chromium` perché lo script `astrobinSync` (integrazione Astro che gira a ogni build, vedi voce del 2026-08-09 sulla cache AstroBin) usa Playwright per interrogare AstroBin — senza il browser installato nel runner CI, qualunque entry con cache scaduta (>24h) farebbe fallire la build.

**Perché:** l'utente vuole valutare GitHub Pages come hosting alternativo/aggiuntivo ad Altervista (che ha mostrato limiti di banda/stabilità, vedi le due voci sopra), partendo dall'URL gratuito prima di impegnare il dominio custom.

**Come si applica:** perché il workflow parta serve (1) push del commit con questi file su `main`, e (2) abilitare "GitHub Actions" come sorgente in Settings → Pages del repo su GitHub (un'azione manuale una tantum, non fattibile da qui). Se in futuro l'utente vuole il dominio custom su GitHub Pages invece che l'URL github.io, vedi la nota sopra su cosa cambiare in `astro.config.mjs` + `CNAME`.

## 2026-08-10 — Due problemi emersi durante il primo deploy reale su GitHub Pages

**1. Build CI falliva, build locale no:** `UNRESOLVED_IMPORT` su `../assets/home/slideshow/Video Project (1).mp4` in `index.astro`. Causa: git tracciava la cartella come `src/assets/home/Slideshow/...` (S maiuscola) ma l'import usa `slideshow` minuscolo — su Windows (filesystem case-insensitive) funzionava comunque, sul runner Linux di GitHub Actions (case-sensitive) no. Fix: `git mv` in due passaggi (verso un nome temporaneo e poi al nome finale) per far registrare a git il cambio di maiuscole/minuscole nel path tracciato, allineandolo alla cartella reale su disco e all'import.

**2. Sito online ma tutti i link interni davano 404** (es. cliccando "About" da `valerioavitabile-cyber.github.io/Astrofotografia/` si finiva su `.../about/` invece di `.../Astrofotografia/about/`). Causa: Astro **non riscrive automaticamente** gli `href` hardcoded nel markup per tenere conto di `base` — lo fa solo per gli asset importati come moduli JS/CSS (per questo `/_astro/...` nei link alle immagini era già corretto). Ogni `<a href="/about/">` scritto a mano nel codice restava letterale, puntando fuori dal sottopercorso `/Astrofotografia/`.

Fix: creato [src/utils/url.ts](../src/utils/url.ts) con un helper `withBase(path)` (usa `import.meta.env.BASE_URL`, lascia invariati URL esterni/`mailto:`) e sostituiti **tutti** gli `href` interni hardcoded del sito con `withBase(...)`: [Header.astro](../src/components/Header.astro) (nav + confronto per lo stato "attivo"), [Footer.astro](../src/components/Footer.astro), [Layout.astro](../src/layouts/Layout.astro) (canonical URL — passato da `site.url` custom-domain a `Astro.site`, coerente col dominio effettivo di deploy; e il confronto `pathname === '/'` negli script `is:inline` per il reset dello scroll in home, iniettato via `define:vars`), 404, index, courses, tutte le pagine portfolio, utility, videos. Anche un link **dentro il markdown** di `about/bio.md` (renderizzato con `marked`) puntava a `/courses/` senza base — fixato con un `.replaceAll` sull'HTML già renderizzato in `about/index.astro`, perché lì non c'è un `href={}` da intercettare.

**Perché:** finché il sito è servito sotto un sottopercorso (github.io/Astrofotografia) invece che dalla root di un dominio, qualunque link scritto come stringa letterale `/qualcosa/` è un bug latente — funziona solo quando `base` è `/` (dominio custom, Altervista). È il tipo di problema che non si vede mai in `astro dev`/build locale se non si naviga davvero il sito cliccando i link (build/lint non lo intercettano).

**Come si applica:** ogni **nuovo** link interno aggiunto al sito d'ora in poi va scritto con `withBase('/percorso/')`, non `href="/percorso/"` letterale — altrimenti si riproduce lo stesso 404 su GitHub Pages (ma non su Altervista/dominio custom, dove `base` è `/` e il bug resta invisibile). Quando l'utente passerà al dominio personalizzato su GitHub Pages, `withBase` continuerà a funzionare senza modifiche (con `base: '/'`, `import.meta.env.BASE_URL` è semplicemente `/` e l'helper diventa un no-op).

## 2026-08-10 — INDAGINE APERTA: hero title/portfolio "già rivelati" al primo caricamento su Android/Chrome reale (non riprodotto in nessun emulatore)

**Stato: non risolto, in corso.** L'utente ha riportato — dopo i fix di overflow titoli/overlay touch sopra — che su un telefono Android reale (Chrome, UA `Mozilla/5.0 (Linux; Android 10; K) ... Chrome/151.0.0.0 Mobile`) la home mostra al **primissimo istante di apertura, prima di qualunque scroll**:
- il titolo hero "Valerio Avitabile" **già rimpicciolito** nell'angolo (dovrebbe partire grande, al centro/basso della sezione hero, e rimpicciolirsi solo scorrendo — vedi il meccanismo `--hero-progress` in [ScrollReveal.astro](../src/components/ScrollReveal.astro) + [Header.astro](../src/components/Header.astro));
- confermato ANCHE con un test "a freddo" pulito (app Chrome chiusa del tutto, riaperta, URL digitato a mano `valerioavitabile-cyber.github.io/Astrofotografia/`, nessun tocco/scroll prima dello screenshot) — quindi **non è** bfcache/cronologia di navigazione, né un link/segnalibro con `#portfolio` in coda (entrambe le piste, verificate e scartate).

Percorso di diagnosi seguito (tutto verificato, tutto scartato tranne l'ultimo passo):
1. **Non è `prefers-reduced-motion`**: l'utente non ha "riduci movimento" attivo (controllato in Accessibilità, nessuna opzione del genere sul suo Android).
2. **Le transizioni CSS funzionano davvero sul suo telefono**: pagina diagnostica pubblicata su `/diagnostics/` (vedi sotto) con un test dal vivo — transizione da 1500ms richiesti misurata a **1512ms reali**. Le CSS transition non sono bloccate a livello di sistema/browser.
3. **Lo scroll e l'IntersectionObserver funzionano**: stessa pagina `/diagnostics/`, estesa con un contatore di eventi `scroll` dal vivo e una replica esatta del meccanismo `.reveal-left` (stesso `rootMargin`/`threshold` di ScrollReveal.astro) — sul telefono reale, 370 eventi scroll ricevuti, box rivelato correttamente solo a scrollY=1973 (non prima). Il meccanismo generico funziona.
4. **Non è drift di scrollY dopo il load**: checkpoint congelati (head sincrono → DOMContentLoaded → load → raf → +300ms → +1000ms) sulla pagina `/diagnostics/` mostrano `scrollY=0` costante per tutto il primo secondo, a freddo.
5. **Non riproducibile in NESSUN emulatore**: replicato l'esatto codice reale (home + ScrollReveal.astro) con Playwright + Chromium in emulazione "Pixel 7" sia in locale (`astro dev`) sia intercettando i log — `--hero-progress`/`getHeroTarget()` resta `target=0.000` per tutti i 4s successivi al load, sia sulla pagina diagnostica sia sulla home vera. **Il bug è quindi specifico del rendering reale di Chrome su Android**, quasi certamente legato al comportamento dinamico della barra degli indirizzi (collassa/appare) su una sezione hero con `min-h-dvh` — un comportamento che nessun browser headless/emulato replica, perché lì il viewport è statico.

**Prossimo passo (dove riprendere):** ho aggiunto un pannello di debug on-page in [ScrollReveal.astro](../src/components/ScrollReveal.astro), attivabile SOLO con `?debug=1` in coda all'URL (nessun impatto per gli utenti normali, invisibile senza il parametro). Registra ogni computazione di `hero-progress` (setup, resize, fonts-ready, primi 5 eventi scroll, +300/1000/2000/4000ms) con timestamp/scrollY/innerHeight in un riquadro fisso in fondo allo schermo. **L'utente doveva aprire `https://valerioavitabile-cyber.github.io/Astrofotografia/?debug=1` sul suo Android e mandare uno screenshot del pannello — non ancora arrivato quando la sessione si è interrotta per la giornata.**

Codice temporaneo di debug lasciato nel repo, da **rimuovere una volta risolto il bug**:
- [src/pages/diagnostics.astro](../src/pages/diagnostics.astro) — pagina standalone (`noindex`) con: check `prefers-reduced-motion`/`hover`/Data Saver, test dal vivo di durata transizione CSS, contatore eventi scroll, replica del meccanismo reveal, checkpoint scrollY congelati. Raggiungibile pubblicamente su `/diagnostics/`.
- `heroDebugRecord()` + tutte le chiamate correlate in [ScrollReveal.astro](../src/components/ScrollReveal.astro) (cercare `debugMode`/`heroDebugRecord`/`heroDebugLog` per trovarle tutte), attivo solo con `?debug=1`.

**Perché è importante ripartire da qui:** l'ipotesi più solida raccolta finora è "comportamento dinamico della barra indirizzi Android + `min-h-dvh`", ma senza il log reale dal pannello debug è ancora **non confermata** — evitare di implementare un fix "alla cieca" basato solo su questa ipotesi; aspettare il dato reale dal dispositivo prima di modificare `getHeroTarget()`/`setupHeroProgress()` in ScrollReveal.astro.

**Come si applica:** alla ripresa, chiedere subito all'utente lo screenshot del pannello `?debug=1` (se non l'ha già mandato). Con quei dati (che riga di log mostra `target` già vicino a 1 e a quale checkpoint) si potrà capire se è davvero un salto della barra indirizzi, un layout-shift causato dal video/dalle immagini hero (`HeroSlideshow.tsx`, `client:load`), o altro. Una volta risolto, ricordarsi di **rimuovere** sia `/diagnostics/` sia tutto il codice `debugMode`/`heroDebugRecord` da ScrollReveal.astro (era esplicitamente temporaneo).

**Aggiornamento 2026-08-11 — Primo screenshot reale arrivato: nuova pista, non più "barra indirizzi Android".** L'utente ha mandato lo screenshot del pannello `?debug=1` sulla home vera. Dato chiave: **tutte** le righe mostrano `scrollY=0` e soprattutto `target=0.000` per tutti i 4 secondi successivi al load (`setup-entry`, `+300ms`, `+1000ms`, `window-load`, `+2000ms`, `+4000ms`) — cioè il calcolo di `getHeroTarget()` dice sempre "hero deve essere grande", eppure visivamente il titolo appare già piccolo nell'angolo fin dal primissimo istante. Questo **esclude l'ipotesi precedente** (drift di `--hero-progress` per via della barra indirizzi dinamica): se il target calcolato resta 0, il problema non può essere nella formula di `getHeroTarget()`.

Osservazione più importante: nel log compaiono **due righe `setup-entry`** (a t=1129ms e t=2831ms, quest'ultima praticamente in contemporanea con `window-load` a t=2836ms) — attese, perché `astro:page-load` (vedi [ClientRouter](../src/layouts/Layout.astro), il sito usa le View Transitions di Astro) fa scattare `setup()` sia al parse iniziale dello script sia di nuovo come evento, e questo è già previsto by design. Ma **manca sempre la riga `setup-applied`** (che `setupHeroProgress()` in [ScrollReveal.astro](../src/components/ScrollReveal.astro) logga subito dopo `setup-entry`, a meno che non esca prima per `if (reduceMotion || !document.getElementById('hero')) return;`) — in **entrambe** le esecuzioni. Questo significa che su questo caricamento reale, il ramo di uscita anticipata scatta sempre: o `reduceMotion` risulta `true` (il che spiegherebbe perfettamente il sintomo, perché la regola `@media (prefers-reduced-motion: reduce)` in [Header.astro](../src/components/Header.astro) fissa **esattamente** `.hero-clone` in posizione angolo/font 16px/opacity 1 in modo statico — combacia al pixel con quello che si vede nello screenshot), oppure `document.getElementById('hero')` restituisce `null` in quel momento (più sorprendente, dato che è markup statico SSR sulla home, ma il sito usa `astro:transitions`/ClientRouter quindi non impossibile in scenari di timing/swap DOM).

Nota: la pagina `/diagnostics/` aveva già un test dal vivo di `prefers-reduced-motion` (`window.matchMedia('(prefers-reduced-motion: reduce)').matches`, sezione "Impostazioni rilevate") che in una sessione precedente aveva dato esito negativo — ma quel test è stato fatto su una pagina/momento diverso, quindi non si può escludere con certezza che sia comunque questo (es. Risparmio Batteria/Data Saver dinamico, o "Rimuovi animazioni" nelle Opzioni sviluppatore Android, impostazione diversa da quella cercata sotto "Accessibilità").

**Azione presa:** invece di ipotizzare alla cieca, aggiunta una riga di log esplicita in `setupHeroProgress()` — `setup-entry rm=${reduceMotion} hero=${!!heroEl} rs=${document.readyState}` — pushata su `main` (commit `a656f09`, deploy GitHub Actions automatico). **Prossimo passo: l'utente deve ricaricare `?debug=1` sul telefono dopo il deploy e mandare un nuovo screenshot** — questa riga dirà in modo definitivo quale dei due rami è la causa, senza bisogno di altre ipotesi.

**Come si applica:** alla ripresa, se `rm=true` nel nuovo log → il sito si comporta correttamente rispetto alla preferenza di sistema rilevata da Chrome, ma quella preferenza sul telefono dell'utente è probabilmente legata a un'impostazione diversa da "Accessibilità" (es. risparmio energetico/dati, opzioni sviluppatore) — da investigare lato utente, non un bug di codice. Se invece `hero=false` → il problema è di timing tra le View Transitions di Astro e l'esecuzione dello script su questo caricamento reale, da indagare in `ClientRouter`/`astro:page-load` (non riproducibile in headless perché lì la sequenza di eventi/tempi è diversa). Non modificare `getHeroTarget()` in nessuno dei due casi: il target calcolato è già corretto (`0.000`), il bug è a monte, nell'esecuzione condizionale di `setupHeroProgress()`.

**RISOLTO (causa trovata) 2026-08-11 — Confermato `rm=true`: non è un bug di codice, è `prefers-reduced-motion` genuinamente attivo sul dispositivo da una fonte diversa da "Accessibilità".** Secondo screenshot del pannello `?debug=1` con la riga aggiuntiva: `setup-entry rm=true hero=true rs=interactive/complete`. Caso chiuso sul fronte diagnosi: `getHeroTarget()` è sempre stato corretto, il ramo di uscita anticipata `if (reduceMotion || ...) return` in `setupHeroProgress()` ([ScrollReveal.astro](../src/components/ScrollReveal.astro)) scatta perché `reduceMotion` è davvero `true` su quel caricamento — e la regola CSS `@media (prefers-reduced-motion: reduce)` in [Header.astro](../src/components/Header.astro) blocca **di proposito** `.hero-clone` in posizione/dimensione finale statica, combaciando esattamente col sintomo visto.

Il "mistero" residuo è solo: **da dove viene quel segnale sul telefono dell'utente**, dato che l'utente non ha trovato nulla sotto Accessibilità. Ipotesi indicate all'utente, in ordine di probabilità: (1) Opzioni sviluppatore → una delle tre "Scala animazione" (finestra/transizione/animator) impostata su "disattivata" invece di 1x — impostazione comune per chi vuole un telefono "più veloce", non etichettata come accessibilità; (2) Risparmio energetico/batteria attivo (Chrome può mappare alcuni power-saver a reduced-motion); (3) su interfacce OEM (Samsung/Xiaomi/ecc.) un'opzione "Rimuovi animazioni"/"Effetti di movimento" fuori da Accessibilità.

**Decisione presa con l'utente:** prima verificare/disattivare l'impostazione lato telefono (comportamento corretto, nessuna modifica al codice) invece di far ignorare al sito la preferenza `prefers-reduced-motion` (opzione scartata per non rompere l'accessibilità per chi la usa intenzionalmente, es. cinetosi/motion sickness). **In attesa di conferma dall'utente** se disattivando l'impostazione trovata sul telefono l'animazione torna normale sulla pagina reale (senza `?debug=1`).

**Come si applica:** se l'utente conferma che disattivando un'impostazione di sistema il problema sparisce → bug chiuso, **rimuovere** `/diagnostics/` e tutto il codice `debugMode`/`heroDebugRecord` da ScrollReveal.astro (era temporaneo, vedi istruzioni sopra). Se invece anche disattivando tutte le impostazioni sospette il problema persiste, tornare a indagare (a quel punto sarebbe utile un modo per l'utente di leggere direttamente il valore `matchMedia` di sistema fuori da Chrome, o testare con un altro browser Android tipo Firefox per isolare se è a livello OS o solo Chrome).

**CHIUSO 2026-08-11 — Confermato dall'utente.** Causa definitiva: **Opzioni sviluppatore Android → "Scala animazione finestra/transizione/animator" impostate a 0x** (comune per chi vuole un telefono percepito come più "scattante"). Chrome mappa quell'impostazione di sistema a `prefers-reduced-motion: reduce`, e il sito rispettava correttamente quella preferenza (comportamento CSS in [Header.astro](../src/components/Header.astro) già corretto). Dopo che l'utente ha rimesso tutte e tre le scale a 1x (e riavviato Chrome completamente, non solo ricaricato la tab — necessario perché `matchMedia` non si aggiorna finché il processo del browser non riparte), l'animazione dell'hero funziona normalmente. Nessuna modifica al codice del meccanismo hero è stata necessaria: **era già corretto**, il problema era solo un'impostazione lato dispositivo mai controllata prima (l'utente aveva controllato solo "Accessibilità", non le Opzioni sviluppatore).

Rimosso tutto il codice temporaneo di debug: la pagina [src/pages/diagnostics.astro](../src/pages/diagnostics.astro) (cancellata) e il blocco `debugMode`/`heroDebugRecord`/`heroDebugEl`/`heroDebugLog` più tutte le sue chiamate sparse in [ScrollReveal.astro](../src/components/ScrollReveal.astro) (log su `setup-entry`, `setup-applied`, `resize-before/after`, `fonts-ready`, `scroll-event-#N`, i timer `+300/1000/2000/4000ms` e `window-load`). Build verificata dopo la pulizia (58 pagine, prima 59 con `/diagnostics/`).

**Perché è stato utile lasciare tracce nel second-brain anche per un "non-bug":** questa indagine (4 sessioni, più aggiornamenti) è un caso di riferimento per bug reali-solo-su-dispositivo che nessun emulatore riproduce — utile ricordare il metodo (pannello debug on-page con log condizionato a un query param, mai in produzione di default) se càpita di nuovo un sintomo "non riproducibile" simile.

**Come si applica:** se in futuro serve investigare un altro bug visibile solo su un dispositivo reale specifico, il pattern di questa indagine è riutilizzabile: (1) pagina/pannello diagnostico temporaneo dietro un query param innocuo (`?debug=1`), mai attivo di default; (2) log con timestamp/stato ad ogni checkpoint rilevante, non solo all'inizio; (3) includere sempre anche i valori delle condizioni di guardia (qui `reduceMotion`/presenza elemento), non solo il risultato finale del calcolo — è stato il dettaglio che ha sbloccato la diagnosi. E controllare le **Opzioni sviluppatore Android** (non solo Accessibilità) quando un utente riporta "nessuna animazione da nessuna parte" su un dispositivo reale.

## 2026-08-11 — Fuse le pagine "Video & tutorial" e "Utility" in un'unica pagina `/video-utility/`

Le due sezioni della home (e le rispettive pagine `/videos/` e `/utility/`) avevano poco contenuto ciascuna (4 video, 3 articoli): su proposta dell'assistente, l'utente ha confermato di unirle in un'unica sezione più ricca, chiedendo un header con link che scrollano alla sezione dedicata invece di due pagine separate.

Creata `src/pages/video-utility/index.astro`: un header con due pill-link ("Video & tutorial" / "Strumenti e guide") che scrollano via ancora a `#video` e `#tools` nella stessa pagina (contenuto invariato, solo riunito). Cancellate `src/pages/videos/index.astro` e `src/pages/utility/index.astro`; le sottopagine `src/pages/utility/{plate-solving,astronomy-tools,polar-alignment}.astro` restano agli stessi URL, solo il back-link ora punta a `/video-utility/#tools`.

Aggiunti redirect statici in `astro.config.mjs` (`redirects: { '/videos/': ..., '/utility/': ... }`) verso `/video-utility/#video` e `/video-utility/#tools`, per non rompere link esterni/bookmark esistenti. **Attenzione:** le destinazioni dei `redirects` di Astro **non** vengono automaticamente prefissate con `base` (a differenza degli `href` generati da `withBase()`) — vanno scritte già complete, es. `/Astrofotografia/video-utility/#video`, altrimenti il redirect prodotto in build punta fuori dal sito (verificato leggendo l'HTML in `dist/` dopo la build).

Nav (`site.ts`) ridotta da due voci a una sola ("Video & Utility" → `/video-utility/`).

Sezione home: primo tentativo con una sola card full-width (le due immagini accostate dentro lo stesso `<a>`) **scartato dall'utente** ("non mi piace"). Ripristinate le due card separate originali (`videoTutorial`/`astronomyTool`, etichette "Video tutorial"/"Utility"), solo i link ora puntano a `/video-utility/#video` e `/video-utility/#tools` invece che a due pagine distinte.

**Perché:** più contenuto percepito per sezione invece di due sezioni "vuote"; un solo salto di navigazione per l'utente finale.

**Come si applica:** eventuali nuovi contenuti video o strumenti/guide vanno aggiunti come nuove voci negli array `videos`/`articles` in `video-utility/index.astro`, non in pagine separate. Se in futuro si aggiungono altri `redirects` in `astro.config.mjs`, ricordarsi di includere `/Astrofotografia` nella destinazione.
